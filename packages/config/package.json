{
  "name": "@nexus/config",
  "version": "1.0.0",
  "description": "Shared configuration utilities for Nexus Trading Platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpm exec tsc",
    "dev": "pnpm exec tsc --watch",
    "clean": "rimraf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "dependencies": {
    "@nestjs/config": "^3.3.0",
    "joi": "^17.13.3"
  },
  "peerDependencies": {
    "@nestjs/common": "^11.0.0",
    "@nestjs/core": "^11.0.0"
  },
  "devDependencies": {
    "@nestjs/common": "^11.0.1",
    "@nestjs/core": "^11.0.1",
    "@types/node": "^22.10.7",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2"
  }
}