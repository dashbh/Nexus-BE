{
  "name": "@nexus/health",
  "version": "1.0.0",
  "description": "Health check utilities for Nexus Trading Platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpm exec tsc",
    "dev": "pnpm exec tsc --watch",
    "clean": "rimraf dist && find src -name '*.js' -o -name '*.d.ts' -o -name '*.js.map' | xargs rm -f 2>/dev/null || true",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "dependencies": {
    "@nestjs/common": "^11.0.1",
    "@nestjs/terminus": "^11.0.0"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2"
  }
}